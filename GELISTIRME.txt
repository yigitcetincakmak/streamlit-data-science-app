with ' in kullanımı anlarsın


import streamlit as st

tab1, tab2, tab3 = st.tabs(["Cat", "Dog", "Owl"])

with tab1:
    st.header("A cat")
    st.image("https://static.streamlit.io/examples/cat.jpg", width=200)
with tab2:
    st.header("A dog")
    st.image("https://static.streamlit.io/examples/dog.jpg", width=200)
with tab3:
    st.header("An owl")
    st.image("https://static.streamlit.io/examples/owl.jpg", width=200)


st.session_state.get('x_kolon'): Daha önce X kutusundan yapılan seçimi alır.


* veri onizlemede groupby komutu ... mesela bu ortalama kısmında içinde groupby olsa mesela yaşın ortalaması olsun birde grupladığı mesela erkeklerin yaş ortalaması gibi

* form ile siteye kayıt olsun veritabanı kullan streamlite in form yapısını kullan

* veri görselleştirmede kullanıcı 2 den fazla kolon seçmek istiyorsa(kullanıcıya sor ve artı butonuna bassın) kolon eklesin belki grafikte kullanır

>>>> Pandas’ta → sayısal veri eksikse "NaN", metin eksikse genelde "None" görürsün ama ikisi de “eksik veri” anlamına gelir.



 #  📌 Sonuç
      #  None kontrolünde daima  "is None"  veya  "is not None" kullan.
      #  "== None"  yerine  "is None"  yazmak hem daha güvenli hem de Pythonic.


                                        VERİ MODELLEME SAYFASI İÇİN:
-------------------------------------------------------------------------------------------------------------------------

 # şu hata sklearn sayısal olmayan sütunlar veriliyor string sütunları oda tahmin yapamıyor one hot encoding istiyor yani
# Kategorik sütunları sayısala çevir (Encoding):

# En çok kullanılanlar:

# LabelEncoder (hedef değişken için)

# OneHotEncoder veya pd.get_dummies() (özellikler için)

#  # Kategorik sütunları dummy (one-hot) olarak dönüştür
#     data = pd.get_dummies(data)


# Hedef değişken için label encoding gerekebilir:
   # if target_df.dtype == 'object':
    #    from sklearn.preprocessing import LabelEncoder
     #   le = LabelEncoder()
      #  target_df = le.fit_transform(target_df)

-------------------------------------------------------------------------------------------------------------------------